{
  "name": "canadian-policyholder-portal",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "concurrently -n server,client -c blue,green \"npm -w server run dev\" \"npm -w client run dev\"",
    "lint": "npm -w client run lint && npm -w server run lint",
    "build": "npm -w client run build && npm -w server run build",
    "test": "npm -w client run test && npm -w server run test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "concurrently": "^8.2.2",
    "husky": "^9.0.11"
  }
}
